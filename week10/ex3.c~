#include <stdio.h>
#include <math.h>
int sum(int n);
void main(){
  int n;
  printf("Input n\n"); scanf("%d",&n);
  if(n<1)
    printf("Error! n must be greater than 1\n");
else
  printf("The sum is: %d\n",sum(n));
}
int sum(int n){
  int i,s=0;
  for(i=0;i<=n;i++)
    s=s+i*i*i;
    return s;
}
